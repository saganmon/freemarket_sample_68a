.user-card__content
  .customer-card__content
    .form_space
      = image_tag"/material/logo/visa-curved.png",size: "50x30"
    .customer-card__content--number.form_space
      %h3
        = "**** **** **** " + @default_card_information.last4
    .customer-card__content--expired.form_space
      - exp_month = @default_card_information.exp_month.to_s
      - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
      %h3
        = exp_month + " / " + exp_year
      = link_to card_path(@card) ,method: :delete,class:"customer-card__content--btn" do
        %button{type: "submit",class: "btn-red"}
          削除する

